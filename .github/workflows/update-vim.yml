name: Extract Vim assets

on:
  schedule:
    - cron: "0 6 * * 0"   # Every Sunday 06:00 UTC
  workflow_dispatch:

permissions:
  contents: write

jobs:
  extract:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y wget dpkg grep

      - name: Get latest Vim version
        id: get_version
        run: |
          BASE_URL="https://packages.termux.dev/apt/termux-main/pool/main/v/vim/"
          wget -qO- "$BASE_URL" | grep -o 'vim_[0-9.]\+_[a-z0-9_]\+\.deb' | sort -V | tail -n1 > latest.txt
          FILE=$(cat latest.txt)
          VERSION=$(echo "$FILE" | sed -E 's/vim_([0-9.]+)_.*/\1/')
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "file=$FILE" >> $GITHUB_OUTPUT
          echo "Detected Vim version: $VERSION"

      - name: Download and extract Vim for all ABIs
        run: |
          set -e
          BASE_URL="https://packages.termux.dev/apt/termux-main/pool/main/v/vim/"
          VERSION="${{ steps.get_version.outputs.version }}"

          declare -A archs=(
            [aarch64]=arm64-v8a
            [arm]=armeabi-v7a
            [i686]=x86
            [x86_64]=x86_64
          )

          rm -rf vim-extract
          mkdir -p vim-extract

          failed_archs=()

          for arch in "${!archs[@]}"; do
            abi="${archs[$arch]}"
            FILE="vim_${VERSION}_${arch}.deb"
            URL="${BASE_URL}${FILE}"

            echo "⬇️ Downloading $URL"
            if ! wget -q "$URL" -O "$FILE"; then
              echo "❌ Failed to download $FILE"
              failed_archs+=("$abi")
              continue
            fi

            mkdir -p "vim-extract/$abi/tmp"
            dpkg-deb -x "$FILE" "vim-extract/$abi/tmp"

            SRC="vim-extract/$abi/tmp/data/data/com.termux/files/usr"
            DEST="$abi"

            if [ -d "$SRC" ]; then
              mkdir -p "$DEST"
              cp -a "$SRC/." "$DEST/"
              echo "✅ Extracted full Vim assets for $abi"
            else
              echo "❌ Extraction failed for $abi (missing usr/)"
              failed_archs+=("$abi")
            fi

            rm -rf "vim-extract/$abi/tmp"
            rm -f "$FILE"
          done

          if [ ${#failed_archs[@]} -ne 0 ]; then
            echo "❌ Failed for architectures: ${failed_archs[*]}"
            exit 1
          fi

          echo "✅ All Vim ABIs extracted successfully."

      - name: Commit and push extracted Vim assets
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          git fetch origin main
          git checkout main
          git reset --hard origin/main

          if [ -n "$(git status --porcelain arm64-v8a armeabi-v7a x86 x86_64)" ]; then
            git add arm64-v8a armeabi-v7a x86 x86_64
            git commit -m "Extracted full Vim ${{
              steps.get_version.outputs.version
            }} assets into ABI directories"
            git push https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{
              github.repository
            }}.git main
          else
            echo "No updates found."
          fi
